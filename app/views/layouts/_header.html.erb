<nav class="navbar" role="navigation" aria-label="main navigation">
<div class="navbar-brand">
    <h1 class="title is-size-2">Members Only</h1>
</div>

<div id="navbarBasicExample" class="navbar-menu">
<div class="navbar-start">
<%= link_to "Index", root_url, class: "navbar-item" %>
    </div>
    <div class="navbar-end">
        <div class="navbar-item">
            <div class="buttons">
                <% if logged_in? %>
                    <%= link_to "Make new post", new_post_path, class: "button is-primary" %>
                    <%= link_to "Log out", logout_path, method: :delete, class: "button is-light" %>
                <% else %>
                    <%= link_to "Log in", login_path, class: "button is-light" %>
                <% end %>
            </div>
        </div>
    </div>
  </div>
</nav>